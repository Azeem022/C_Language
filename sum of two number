#include<stdio.h>
int main()
{
	int a,b;
	printf("entre the value of a:");
	scanf("%d",&a);
	printf("entre the value of b:");
	scanf("%d",&b);
	int sum;
	sum=a+b;
	printf("sum is :%d",sum);
	return 0;
}
